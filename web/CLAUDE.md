# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

@AGENTS.md

## Commands

```bash
npm run dev      # start dev server at localhost:3000 (Turbopack)
npm run build    # production build — run this to catch type errors across the app
npm run lint     # eslint
npx tsc --noEmit # type-check only, faster than a full build
```

There is no test suite configured yet.

Node.js is not installed via Homebrew on this machine (no `brew`) — it was manually installed to `~/.local/node` and added to `PATH` via `~/.zshrc`. Don't assume `brew`/`nvm` are available for tooling changes.

## Source of truth

`../We-Present-Website-Technical-Specification-v1.8.docx` (one directory above `web/`, outside this project) is the client's technical spec and drives design, copy, and information-architecture decisions — palette, fonts, sitemap, form fields, tour dates, etc. v1.8 folds in the client's first round of live-site feedback (three chat messages plus Daria's page-by-page text revisions and the invitation copy) — see its own "Key changes since v1.7" section and Appendices A–D for the exact brand-naming rules and replacement copy. It's a binary `.docx`; read it with:

```bash
textutil -convert txt -stdout "../We-Present-Website-Technical-Specification-v1.8.docx"
```

The raw resort photo/video/logo libraries (e.g. `../Fushifaru Images and Videos/`) also live one directory up and are **not** part of this repo (excluded by the top-level `.gitignore` — they're 5–17GB each). `public/images/` contains only a curated, resized subset copied in via `sips -Z <maxdim> -s format jpeg -s formatOptions 82`. When adding imagery for a new resort or section, pull source files from those sibling folders the same way rather than committing raw originals.

## Architecture

Next.js 16 App Router + TypeScript + Tailwind v4 + Framer Motion. All routes live under `src/app/[locale]/` — `/[locale]`, `/[locale]/about`, `/[locale]/tours`, `/[locale]/tours/[slug]`, `/[locale]/partners`, `/[locale]/contact`, `/[locale]/register`, `/[locale]/become-a-partner`, `/[locale]/how-it-was`, `/[locale]/legal`, `/[locale]/resorts/[slug]` — the full spec sitemap is now built out for both `ru` and `en`. `Resorts` is the only nav item still an anchor (`/[locale]/#resorts` on the home page) since the spec has no standalone resorts-listing page — the grid lives on Home.

The Legal page's privacy-policy copy (`dict.legalPage`) is an honest first-pass draft, not lawyer-reviewed — it says so on the page itself (`draftNotice`) and is written to describe what the site *actually* does right now (forms → Coati team + local log, no analytics yet) rather than the eventual AWS/S3/analytics end-state from the spec, so it doesn't overclaim infrastructure that doesn't exist yet. Update it as real infra (S3 uploads, Yandex.Metrica/GA, defined retention periods) comes online — don't just relax the draft-notice wording without also updating the substance.

The How It Was page intentionally has no images — the spec (Section 6.9) calls for photo galleries, trip reports, and reviews, but none exist yet since 2026 is the launch season. It follows the same "elegant coming-soon, never looks empty" pattern as the Home teaser, just with a full 4-section breakdown (Photo Galleries / Trip Reports / Agent Reviews / Hotel Partner Reviews) instead of one card. When real tours complete, this is the page to convert from static dictionary copy to portal-managed content.

**Design tokens** live in `src/app/globals.css` as CSS custom properties (`--color-ivory`, `--color-aubergine`, `--color-gold`, etc., matching the spec's palette exactly) re-exposed to Tailwind via `@theme inline`, giving utilities like `bg-ivory`, `text-amethyst`, `bg-aubergine`. Fonts are Playfair Display (`font-display` class, headings) and Manrope (default body sans), both loaded through `next/font/google` in `src/app/layout.tsx`.

**Resort data model** (`src/lib/resorts.ts`) is the single source of truth for the 4 resorts — including `heroImage`, `heroVideo`, `story` paragraphs, `keyFacts`, and `gallery` array, so `src/app/resorts/[slug]/page.tsx` is fully generic with no per-resort hardcoding. Each entry also has a `built: boolean` and `logoBg: "none" | "dark"` flag:
- `built: false` would render as a non-linked "Coming Soon" card in `ResortsGrid` and skip `generateStaticParams` (404 via `notFound()`) — all 4 are currently `true`, but the flag stays live for adding a 5th resort later without a code change.
- `logoBg` exists because the delivered logo files aren't uniform: some are transparent wordmarks/marks that read fine directly against any light section background ("none" — SO/Maldives, Fushifaru), one already has its own solid-black background baked into the PNG itself ("none" — Meyyafushi, since the image *is* the card, no wrapper needed), and one is light-colored art on a transparent background that's invisible without a dark backdrop ("dark" — Madifushi, wrapped in a small aubergine pill). `PartnersStrip` and the `/partners` page both branch on this flag. Logos render at a shared height via a fixed bounding box (`relative h-14 w-32` + `fill` + `object-contain`) rather than sized to each logo's own aspect ratio — an earlier attempt at "auto-width, shared height" made the extremely wide SO/Maldives wordmark dwarf the square marks next to it. Check a new resort's actual logo file (transparency, aspect ratio, background) before assuming a default.

**Hero videos**: each resort's hero section plays a short (~7-8s) looped, muted `<video>` cut from that resort's official brand video, with `heroImage` as the `poster` fallback. Source footage lives in the sibling raw asset folders (e.g. `../Fushifaru Images and Videos/FF Brand Video.mov`, 4K/200MB+) and is NOT in the repo; `public/videos/<slug>-hero.mp4` holds only the trimmed, 1080p, ~3-6MB encoded output. `ffmpeg` isn't installed on this machine — it's pulled in as the `ffmpeg-static` npm package (`node_modules/ffmpeg-static/ffmpeg`), not a system binary. To re-cut a clip: extract 1fps preview frames (`ffmpeg -i src -vf fps=1,scale=320:-1 out_%02d.jpg`) or tile them into one contact-sheet image (`-vf tile=8x8`) to find a good window fast, since these source videos are 30s-2min and a good moment isn't obvious from the filename. Then trim + re-encode: `ffmpeg -ss <start> -i src -t <duration> -vf "scale=1920:1080:force_original_aspect_ratio=increase,crop=1920:1080" -an -c:v libx264 -crf 26 -preset veryfast -movflags +faststart out.mp4`. Watch for burned-in captions in the source footage (Meyyafushi and Madifushi's official videos have on-screen text like "A touch of tranquility" in some sections) — avoid those windows since our own page copy overlays the video already.

**Tour data model** (`src/lib/tours.ts`) mirrors this pattern for `/tours` and `/tours/[slug]` — `status: "pending"` tours (Oman, 2027) render as non-linked cards; `generateStaticParams` only builds detail pages for `"confirmed"` tours. Tour stops optionally reference a `resortSlug` to link into the resort pages. The home page's `ToursTimeline` reads from this same file rather than duplicating tour data — always add new tours here, not inline in a component.

To add a 5th resort: add curated images to `public/images/resorts/<slug>/` (see the `sips` pattern below), add its full entry to `resorts.ts` (`heroImage`, `story`, `keyFacts`, `gallery`), done — no page code changes needed.

**`Reveal`** (`src/components/Reveal.tsx`) wraps Framer Motion's `whileInView` fade/slide-up and is the standard scroll animation used across every section — reuse it rather than hand-rolling `motion.div` calls, to keep the "slow, refined" motion feel consistent.

## Internationalization (RU/EN)

`src/proxy.ts` (Next 16 renamed `middleware.ts` → `proxy.ts`, same API — see AGENTS.md) redirects any request without a `/ru` or `/en` prefix to `/ru` (default locale, per spec Section 7). `src/i18n/config.ts` defines `locales`, `defaultLocale`, and the `href(locale, path)` helper — **every internal `Link`/`Button` href must be built with `href()`** so it carries the locale prefix; a bare `"/tours"` string will silently drop the user out of their locale.

There is no separate root `layout.tsx` — `src/app/[locale]/layout.tsx` **is** the root layout (sets `<html lang={locale}>`, loads fonts, renders `Header`/`Footer`), since the only top-level route segment is `[locale]`. It calls `generateStaticParams()` to pre-render both locales.

**Two-tier translation content:**
- UI chrome and page copy (nav, buttons, kickers, paragraphs) live in `src/i18n/dictionaries/en.ts` and `ru.ts`. `en.ts` is canonical; `ru.ts` is typed as `Dictionary` (`typeof en`) so a missing key is a compile error, not a silent English fallback. Look up via `getDictionary(locale)` — cheap, synchronous, safe to call in both Server and Client Components (client forms like `InquiryForm`/`ContactForm` take a `locale` prop and call it directly).
- Per-entity content (resort stories/key facts/taglines in `resorts.ts`, tour summaries/stop notes in `tours.ts`) is localized in place as `{ en: ..., ru: ... }` objects, read via each file's own `t()`/`tl()` helper. This is deliberately separate from the dictionaries file since it's data, not UI copy.

**The Russian copy throughout is a first-pass machine/LLM translation** (including `ru.ts` and all resort/tour RU content) — functionally correct and natural-reading, but written without a native Russian-market travel-trade reviewer. Flag this to the client before launch; don't treat it as final.

Cyrillic glyph coverage for Playfair Display and Manrope was verified for real (built and screenshotted a throwaway route rendering Cyrillic text in both fonts before committing to this approach) — safe to keep building on these font choices.

**Known CSS gotcha**: don't let an element with `position: fixed` live inside an ancestor that has `backdrop-filter`/`filter`/`transform` — it changes the fixed element's containing block from the viewport to that ancestor's box, which can silently zero out its size. This bit the mobile nav overlay (it was a child of `<header>`, which has `backdrop-blur-sm`); fixed by rendering the overlay as a sibling of `<header>` instead of a descendant (see `src/components/Header.tsx`).

**Header nav breakpoint**: the full desktop nav (6 links + Become a Partner + Register Interest + language switch) only fits comfortably from `xl` (1280px) up — it was originally `lg` (1024px), but at 1024-1279px the logo's "BY COATI TRAVEL" byline wraps and collides with the first nav link. The hamburger menu now covers everything below `xl`. If you add another nav item, re-check this breakpoint with real screenshots at 1024/1279/1280/1440 rather than assuming — it's tight even at 1280.

## Forms

All four forms (`InquiryForm`, `ContactForm`, `RegisterForm`, `PartnerForm`) POST to their own route under `src/app/api/{inquiry,contact,register,partner}/route.ts`, which all call `sendSubmission()` in `src/lib/mailer.ts`. That function always appends the submission to `.submissions/submissions.log` (gitignored) — a local stand-in for the spec's "append every submission to a Google Sheet" backup (Section 9.1) — then, if `GMAIL_USER`/`GMAIL_APP_PASSWORD` env vars are set, sends a real email via Gmail SMTP (Nodemailer) to `NOTIFY_EMAIL` (defaults to `wepresentproject@gmail.com`, a temporary test inbox — will change before real launch). Without those env vars it degrades gracefully: submission still succeeds and logs locally, no email sent. See `.env.local.example` for the exact vars; `GMAIL_APP_PASSWORD` requires 2-Step Verification enabled on the Gmail account first (myaccount.google.com/apppasswords).

**File uploads** (Register's statistics + business card, Partner's images/brochure) are validated server-side in `src/lib/uploads.ts` — extension whitelist (pdf/jpg/jpeg/png/doc/docx/xls/xlsx) and a 15MB cap, per spec Section 8.4. Files are read from the route's `request.formData()` (native Web API, not JSON — these two routes are the only ones that take `multipart/form-data`), saved to `.submissions/uploads/` alongside the log entry, and passed to `sendSubmission()` as Nodemailer `attachments` so they arrive on the notification email directly. This is a stopgap for the spec's private-S3-bucket-plus-secure-link approach (Section 8.4) — fine for testing, not for production launch volume.

**Anti-spam**: all four routes (`inquiry`, `contact`, `register`, `partner`) check a hidden `company_website` honeypot field (rendered off-screen in the form, named plausibly so bots fill it) — if present, the route returns a normal-looking `{ok: true, delivered: false}` without logging or emailing, so bots get no signal they were caught.

**Rate-limiting** (spec Section 8.4): `src/lib/rateLimit.ts` is a small in-memory, per-IP, per-route limiter (5 submissions per 15 minutes, sliding-window-by-reset) — all four routes call `checkRateLimit()` first thing, before parsing the body, and return `429` with a `Retry-After` header once exceeded. It's keyed off `x-forwarded-for`/`x-real-ip`, so it needs a real proxy/host setting that header correctly to identify clients (works out of the box on Vercel/Netlify/most PaaS; verify on whatever host is finalized). **This is in-memory and per-process** — it resets on every deploy/restart and doesn't share state across multiple instances (serverless, multi-region, or a load-balanced cluster would each get their own independent counter). Fine for this site's current scale and a single-Node-process hosting setup; swap for a shared store (Redis/Upstash/DynamoDB) if traffic or hosting topology changes before launch.

## Impeccable audit (homepage, 2026-07-18)

Ran the `impeccable` skill's `audit` flow manually against the homepage (13/20, "Acceptable" band — full report in conversation history, not duplicated here). The three P1s are fixed:
- **Gold text on light backgrounds** (`ToursTimeline.tsx` year label, `ResortsGrid.tsx` "Explore →" label) measured 2.3:1 contrast, failing WCAG AA — swapped to `text-amethyst` (6.09:1). This also matches the spec's own Section 4.1, which reserves gold for fills/dividers/rules, never small text on light backgrounds — don't reintroduce `text-gold` for body-sized text anywhere.
- **`Reveal` shipped invisible without JS**: every `Reveal`-wrapped element (nearly all homepage content) starts at `opacity: 0` in the server-rendered HTML and only becomes visible once JS hydrates and `whileInView` fires — a no-JS or failed-hydration visitor saw a blank page. Fixed with a `<noscript>` override in `app/[locale]/layout.tsx` targeting a `data-reveal` attribute now set on every `Reveal` instance (`[data-reveal] { opacity: 1 !important; transform: none !important; }`). Verified with a real JS-disabled Playwright context, not just code review — keep that verification method if you touch `Reveal` again, since a visual check with JS on won't catch this class of bug.
- **Missing `sizes` prop** on every `fill` image on the homepage (Hero, `ResortsGrid` ×4, `PartnersStrip` ×4) — Next.js was serving the largest breakpoint image regardless of actual rendered size. Added per-component: `sizes="100vw"` for the full-bleed hero, `sizes="(min-width: 1024px) 25vw, (min-width: 640px) 50vw, 100vw"` for the 4-column resort grid, `sizes="144px"` for the small fixed-size partner logos. Any new `fill` image needs a `sizes` prop matching its actual responsive rendered width — this was flagged by a real dev-server console warning, not just the audit.

The P2/P3s are fixed too:
- **Mobile hamburger tap target**: was ~28×15px (no padding around the three lines); now `min-h-11 min-w-11` + centering gets it to a verified 44×44px without changing the visual icon size.
- **Mobile nav missing ARIA/landmark**: the toggle button now has `aria-expanded`/`aria-controls="mobile-nav"`, and the overlay is a `<nav id="mobile-nav" aria-label="Mobile menu">` instead of a plain `<div>` — desktop and mobile nav are now both real landmarks.
- **Marginal contrast on `text-ink/60` / `text-soft-lilac/60`**: bumped to `/70` in `ResortsGrid.tsx`, `ToursTimeline.tsx`, `HowItWas.tsx`, and `Footer.tsx` (4.06:1 → 5.52:1, 3.96:1 → 4.81:1). This was only fixed in these homepage-scoped components — the same `/60` pattern likely recurs on the resort/tours/about pages if you want a broader sweep later.
- **`ConceptSection` missing heading**: added a `sr-only` `<h2>` (`dict.home.concept.sectionLabel`) so it appears in the screen-reader heading outline without changing the visual design.
- **Generic resort card alt text**: added a `cardImageAlt: string` field per resort in `resorts.ts` (scene-descriptive, e.g. "Aerial view of Fushifaru Maldives, rows of overwater villas stretching into the lagoon") — `resort.name` is still used for the `name` field itself (headings, logo alt, etc.), just no longer doubles as image alt text.

All fixes verified live in a real browser (computed styles, bounding boxes, ARIA attribute values, heading outline via `document.querySelectorAll`), not just code review.

## Impeccable audit (resort pages, 2026-07-18)

Ran the same `impeccable` audit flow against the shared resort template (`app/[locale]/resorts/[slug]/page.tsx`, covers all built resorts, scored against `fushifaru`: 11/20). The three P1s are fixed:
- **Hero video ignored `prefers-reduced-motion`**: the autoplay/loop hero video played regardless of the visitor's motion preference. Extracted the video-vs-image branching out of the page into a new `ResortHeroMedia.tsx` client component that subscribes to `matchMedia("(prefers-reduced-motion: reduce)")` via `useSyncExternalStore` (not a `useEffect` + `setState`, which the lint rule flags as a cascading-render risk) and renders the static `heroImage` instead of `<video>` when motion is reduced. Verified with a real `reducedMotion: "reduce"` Playwright context showing no `<video>` element in the DOM at all, vs. a normal context where it renders and autoplays.
- **`text-ink/40` contrast failure**: found on the Key Facts "Official Website" value, and — not caught in the original audit pass, found via a follow-up grep once fixing the first — the bottom "All Resorts" nav link too. Same defect, same fix: both bumped to `text-ink/70`. If you see `text-ink/40` anywhere else in the codebase, treat it as the same known-bad pattern.
- **`Gallery.tsx` missing `sizes` prop** on both `fill` images (grid thumbnails and the lightbox) — same class of bug as the homepage audit's `sizes` finding, just missed there since `Gallery` isn't rendered on the homepage. Added `sizes="(min-width: 640px) 33vw, 50vw"` for grid thumbnails, `sizes="90vw"` for the lightbox. Verified both attributes are present on the actual rendered `<img>` elements, including inside the opened lightbox.

The P2/P3s are fixed too:
- **Gallery lightbox close button tap target**: was ~14.6×36px; now `flex min-h-11 min-w-11 items-center justify-center` gets it to a verified 44×44px (repositioned slightly from `right-6 top-6` to `right-4 top-4` so the glyph still sits close to the same visual corner).
- **Prev/Next resort nav links + "All Resorts" link**: were ~20px tall text-only links; all three now use `inline-flex min-h-11 items-center`, verified at a real 44px bounding-box height.
- **Key Facts panel had no heading of its own**: added a `sr-only` `<h2>` (`dict.resortPage.keyFacts`, already existed as a string — no new dictionary key needed) right before the existing `Kicker`, same pattern as the homepage's `ConceptSection` fix.
- **`InquiryForm`'s heading was an orphaned `h3`**: promoted to `h2` since it's the only heading in its section and `InquiryForm` is only ever rendered here. Confirmed via grep it isn't used anywhere else, so this doesn't create a hierarchy problem elsewhere. Verified page's full heading outline is now a clean H1 → H2 (Story) → H2 (Key Facts) → H2 (Gallery) → H2 (Inquiry), no skipped or orphaned levels.

All fixes verified live in a real browser (heading outline via `document.querySelectorAll`, bounding boxes for tap targets, a lightbox screenshot to confirm the larger close button didn't look visually off), not just code review.

## Impeccable audit (tours pages, 2026-07-18)

Ran the same audit against `tours/page.tsx` and `tours/[slug]/page.tsx` (16/20, "Good" band). The three P1s — all the same recurring contrast defects already fixed elsewhere, just in the two files that hadn't been touched yet — are fixed:
- **`text-gold` on light backgrounds**: the "2026"/"2027" year-group labels (measured 2.08:1) and the itinerary stop-date kicker on the tour detail page (measured 2.30:1) — both swapped to `text-amethyst`, matching the homepage/resort-page fix and the spec's own rule (gold is for fills/dividers, never small text on light backgrounds).
- **`bg-gold/15 text-gold` status badges** (the "pending"/"coming soon" tour badges, measured 2.01:1 — worse than plain text since the badge background is itself a faint gold tint): kept the `bg-gold/15` fill (gold-as-fill is fine per spec) but changed the text to `text-amethyst` (5.32:1 against that badge background). This still visually distinguishes "pending" (warm gold-tinted badge) from "confirmed" (cool amethyst-tinted badge) — only the badge background differs now, not the text color — so the fix didn't erase the status distinction.
- **`text-ink/60`**: tour summary text, itinerary stop notes, and the 2027 coming-soon body copy (measured 4.00–4.02:1) — all bumped to `/70`, same fix as every other page. Verified live: computed colors for all six changed elements resolve to `rgb(110, 79, 163)` (`#6e4fa3`, amethyst) or `ink` at 0.7 alpha as expected. No images on these two page types, so there was no `sizes`-prop class of bug to find here — a genuine difference from the homepage/resort audits.

Two P2 findings from this pass are **not yet fixed** (only do if asked): the "2026"/"2027" year groupings are `<p>` tags rather than headings (no way to jump to a specific year via heading navigation), and the TTM overview section on the Tour 2 detail page has no heading at all (confirmed via live heading-outline check: H1 jumps straight to H2 "The full itinerary", skipping that whole block). Also one P3: the non-interactive "Oman" (pending-status) card still gets the same `hover:-translate-y-1 hover:shadow-md` as the clickable confirmed-tour cards, implying interactivity it doesn't have.

The P2/P3s are fixed too:
- **Year groupings weren't real headings**: `<p>` → `<h2>` for "2026"/"2027". Since the year now outranks the tour cards it groups, each tour's own name heading was demoted `<h2>` → `<h3>` (and the "Coming Soon" 2027 placeholder title, too) so the outline nests correctly instead of having year and tour-name at the same level. Verified live: full outline is now H1 → H2 (2026) → H3 (each tour) → H2 (2027) → H3 (Coming Soon), no siblings-that-should-be-parent-child.
- **TTM overview section had no heading**: added a `sr-only` `<h2>` reusing the existing `dict.tourDetail.ttmKicker` string ("TTM Maldives 2026") — no new dictionary key needed, same pattern as the Key Facts panel fix on the resort pages.
- **"Back to All Tours" link tap target**: was 71×19px; now `inline-flex min-h-11 items-center` gets a verified 44px-tall target.
- **Oman card's misleading hover affordance**: `hover:-translate-y-1 hover:shadow-md` is now applied conditionally (`tour.status === "confirmed"`) instead of unconditionally on the shared `card` JSX, so the non-clickable pending-status card no longer invites a click that does nothing. Verified live by diffing the Oman card's class list against a confirmed tour's — only the confirmed one carries the hover classes now.

All fixes verified live in a real browser (full heading outlines via `document.querySelectorAll`, class-list diffing for the hover-affordance fix, a bounding-box check for the tap target), not just code review.

## Client feedback round 1 (v1.8 spec), 2026-07-21

Implemented the unblocked portion of the v1.8 spec's Appendix C checklist — everything that didn't depend on assets still pending from the client (logo SVG, selected hero photo, hotel URLs, footer contact details, TTM tier naming). Covers `en.ts`/`ru.ts`, `resorts.ts`, `tours.ts`, `Header.tsx`, and most home/partners/contact/about components.

- **Brand rename**: "Coati Travel" removed everywhere (dictionaries, forms, legal copy) per Appendix A — "COATI Global Sales Agency" in body/prose, "Coati" in small UI (buttons, kickers, footer credit, `nav.byCoati` — renamed from `nav.byCoatiTravel`). Footer parent-brand block is now "Эксклюзивная инициатива / Проект Coati" (EN: "An Exclusive Initiative / A Project by Coati"), replacing "Материнская компания / Проект Coati Travel →". The `coatitravel.ru` footer/contact link itself is untouched — the spec lists the correct parent-brand link/domain as still pending from the client.
- **Madifushi**: atoll corrected "Thaa Atoll"/"Атолл Тхаа" → "Meemu Atoll"/"Атолл Меему" everywhere (hero, key facts, story, EN+RU). Hero/card image replaced with the client's supplied aerial (`public/images/resorts/madifushi/hero.jpg` + `card.jpg`, resized via `sips` from the source `../MPI -Aerials (4).jpg`, which is gitignored — same "curated copy in repo, raw source outside" pattern as the other resorts); the old staircase shot is preserved as a gallery image (`steps.jpg`) rather than deleted.
- **Tour 2**: dropped "inaugural tour" framing (EN+RU) for neutral wording per the client. Added two new pending/coming-soon entries to `tours.ts`: **Cinnamon Resorts Maldives** (Oct 2026) and **Kenya** (dates TBC, grouped under its own "2026 / 27" year on `/tours` since it isn't confirmed for a single season).
- **RU-only label changes** (per spec Appendix B.1 — EN nav parity was explicitly flagged "confirm with client" and left untouched): `nav.about` "О программе"→"О проекте", `nav.tours` "Туры"→"Программы", hero pill "Ближайший тур"→"Ближайшая программа", tours page title drops "— коротко", coming-soon-2027 body → "Готовим новые направления".
- **New home-page intro block** (`IntroSection.tsx`, between Hero and ConceptSection): destination-neutral "what is WePresent" paragraph sourced from the client's invitation (spec Appendix B.6), addressing the client's "not clear what WePresent is about" + "site looks Maldives-only" feedback. The Maldives-specific hero headline/lead itself was left as-is (Daria-approved copy, and the spec explicitly allows it to stay for the current season) — the intro block is what carries the multi-destination framing.
- **Contrast pass** (client: "white and lilac look similar", "elements blend into the background"): swapped card/panel backgrounds from `bg-lavender-mist/40` to `bg-soft-lilac` (with a hairline border/shadow) on `ResortsGrid`, the Partners page resort cards, and the `HowItWas` coming-soon cards, per spec §4.1 ("soft-lilac is now the default panel colour"). Added a `border-t border-amethyst/10` at every ivory↔lavender-mist band boundary on the homepage for section-rhythm separation. Bumped every remaining `text-ink/60` and `text-ink/40` to `/70` site-wide (forms, About, Contact, Partners, How It Was — the Partners page hadn't been through the impeccable sweep yet and still had the old pattern). Found and fixed two additional real AA failures the initial pass introduced/exposed, both verified live via canvas-based contrast computation (not just code review, since Tailwind v4's oklab colors don't parse as plain rgb): `ToursTimeline.tsx`'s pending-tour badge was still `text-gold` on `bg-gold/15` (2.0:1) — swapped to `text-amethyst` matching the already-fixed `/tours` page pattern; and `text-amethyst/70` kickers (resort key-facts labels, Partners/ResortsGrid atoll labels, Contact page labels) measured 2.9–3.2:1 once sitting on the new soft-lilac card backgrounds — bumped to full-opacity `text-amethyst` (verified 5.0–6.1:1 across all affected instances).
- **Partners logos**: bigger (`h-16 w-36` → `sm:h-20 sm:w-44`, up from `h-14 w-32`) and given a consistent bordered card treatment on both the homepage strip and `/partners` page. Added an optional `website?: string` field to the `Resort` type (spec §6.7's "clickable, links to the hotel's own site") — logos render as a plain `<a target="_blank">` only when a resort has a `website` set; none do yet, since hotel URLs are still pending from the client (spec Appendix D). Add the URL to the relevant `resorts.ts` entry once received — no other code change needed.
- **Mobile/tablet header bug** (client: "byline text under the logo is misplaced on mobile, should look like the English version"): root cause found live, not guessed — at exactly the `xl` (1280px) breakpoint where the desktop nav appears, the Russian nav labels are ~120px wider in total than the English ones (longer Cyrillic words), which left too little room for the logo lockup and wrapped "ОТ COATI" onto its own line under "WE PRESENT" — English never wrapped at the same width. Fixed by moving the full-nav breakpoint from `xl` to a custom `min-[1400px]` throughout `Header.tsx` (nav, both CTA buttons, language switch, hamburger toggle, mobile overlay), which gives enough headroom for the wider Russian nav. Verified via bounding-box checks at 1280/1320/1360/1400/1440/1500px in both locales — logo never wraps at any width now, nav appears only once there's room for it.

Not investigated further: the client's broader "Russian version doesn't display correctly" report (misalignment, overlap) beyond this specific header bug — no other RU-specific rendering defect turned up in a full-page live check at desktop/tablet/mobile widths in this pass, and both fonts already load full Cyrillic subsets (`subsets: ["latin", "cyrillic"]` in `layout.tsx`, previously verified). If the client points to a specific page/section still, check that page's real Russian string lengths at its actual breakpoints rather than assuming a font-coverage issue — the one confirmed RU-only bug so far (the header) had nothing to do with font coverage, despite the client's own guess that it did.

## Impeccable audit (partners page, 2026-07-21)

Ran the full `impeccable` audit flow against `/partners` (17/20, "Good" band). Two findings, both fixed (P3s deferred, not requested):
- **[P1] `text-gold` badge on light background**: the "TTM Maldives · 10th Edition" badge (`partnersPage.associated.badge`) measured 2.30:1 — the same recurring contrast defect from every prior audit round, just not yet swept on this page. Swapped to `text-amethyst`, verified live at 6.09:1.
- **[P2] Resort cards were `<a>` with no `href`**: since no resort has a `website` yet, `href={resort.website}` rendered as `href={undefined}` — an anchor with no `href`, invalid semantics wrapping non-interactive content. Fixed by mirroring `PartnersStrip.tsx`'s existing conditional: renders a plain `<div>` when `resort.website` is absent, `<a target="_blank">` only once it's set. Verified live: 0 hrefless anchors, all 4 cards currently `DIV` (will automatically become `A` the moment a `website` URL is added to `resorts.ts` — no further code change needed then).

The two P3s are fixed too:
- **Redundant alt-text/heading double-announcement**: the resort logo's `alt` duplicated the visible `<h3>` resort name, so a screen reader would announce the name twice once a card becomes a real link. Set the logo `alt=""` (decorative — the heading already names the card); the link's accessible name now reads as one clean pass over the heading, atoll and tagline instead of the name twice. Left `aria-label` off deliberately — `PartnersStrip.tsx`'s image-only logos need it since they have no visible text, but adding it here would have suppressed the atoll/tagline from the accessible name, a net loss of information for screen-reader users navigating by link.
- **Un-tokenized shadow value**: promoted the repeated `shadow-[0_1px_2px_rgba(62,44,85,0.06)]` (partners cards + `ResortsGrid.tsx`) to a `--shadow-card` theme token in `globals.css`, used as the `shadow-card` utility. Verified both usages resolve to the identical computed `box-shadow` value post-refactor.

## Impeccable audit (remaining pages, 2026-07-21)

Ran the full audit against the six pages that hadn't been through the cycle yet: About (15/20), How It Was (20/20), Register (17/20), Become a Partner (17/20), Contact (18/20), Legal (20/20). How It Was and Legal were already essentially clean; the rest had these fixed (no P3s were raised this round):

- **[P1] Gold step numbers on About page**: the "01/02/03" selection-model numbers (`about/page.tsx`) measured 1.97:1 — fails even the large-text 3:1 floor. Swapped `text-gold` → `text-amethyst`, verified 5.22:1. Same recurring defect as every other page audited so far (six rounds running now); worth a repo-wide grep at some point instead of catching it page-by-page.
- **[P1] `FileField.tsx`'s "no file selected" text**: `text-ink/50` measured 3.04:1 — a distinct opacity value the earlier `/60`/`/40` → `/70` sweep didn't catch since it only matched those two exact values. Bumped to `/70` (5.51:1). Shared by both Register and Become a Partner, fixed once for both.
- **[P2] Missing `sizes` prop on `AlternatingBlock.tsx`'s fill image** (used on the About page, 2 instances): defaulted to `100vw` despite rendering at ~half-width on desktop — the same class of bug as the very first homepage audit's `sizes` finding, just not caught here since About wasn't in scope yet. Added `sizes="(min-width: 1024px) 50vw, 100vw"`.
- **[P2] Orphaned H3 on Register and Become a Partner**: both `RegisterForm.tsx` and `PartnerForm.tsx` render their form title as `<h3>` with no `<h2>` anywhere on the page (H1 → H3 skip) — promoted both to `<h2>`.
- **[P2] Contact page had no heading at all** beyond its `H1`: the details block only had a `Kicker` (a styled `<span>`, not a heading), and `ContactForm` had zero heading elements since `forms.contact` was the only form dictionary entry missing a `.title` string. Added `forms.contact.title` (EN "Send Us a Message" / RU "Отправить сообщение") + `<h2>` in `ContactForm.tsx`, and a `sr-only` `<h2>` reusing `contactPage.details.kicker` for the details block. Outline is now a clean `H1 → H2 → H2`.
- **[P2] File input focus indicator**: the native `<input type="file">` in `FileField.tsx` is `h-0 w-0 opacity-0` (still tabbable, just invisible), so keyboard focus produced no visible indicator at all. Added `has-[:focus-visible]:border-gold has-[:focus-visible]:ring-2 has-[:focus-visible]:ring-gold/30` to the visible wrapper div — verified live by focusing the input directly and reading the wrapper's computed border-color/box-shadow.

All fixes verified live (contrast via canvas compositing, heading-outline via `document.querySelectorAll`, focus style via programmatic `.focus()` + computed style). This closes out the full audit sweep — every page in the sitemap has now been through the audit → P1 → P2/P3 → verify cycle.

## Mobile spot-check (2026-07-22)

Did a fresh mobile pass (390×844, Playwright — the `resize_window` browser-automation tool doesn't reliably resize the actual rendered viewport in this environment, `document.documentElement.clientWidth` stayed desktop-sized regardless of requested width, so headless Playwright is the reliable way to check mobile here) across home, tours, partners, a resort detail page, register and contact: no horizontal overflow anywhere, mobile nav overlay opens cleanly, the new intro section and 5-tour calendar both read well at mobile width, and the Madifushi hero photo/gallery look right.

One real miss found and fixed: **`contactPage.details.parentBrand`** still said "Материнская компания"/"Parent Brand" — the brand-rename round fixed `footer.parentBrand` (same concept, same wording) but missed this separate dictionary key that renders the identical label on the Contact page's own details list. Now reads "Эксклюзивная инициатива"/"An Exclusive Initiative", matching the footer.

## Repo-wide `text-gold` sweep (2026-07-21)

Grepped every remaining `text-gold` usage in `src` (7 hits) to close out the recurring contrast defect once and for all, rather than keep finding it page-by-page:
- **4 in `Footer.tsx`** (section-label kickers) — solid `bg-aubergine`, computed **5.16:1**. Fine, no change.
- **1 in `Kicker.tsx`** — the `tone="gold"` branch. Confirmed via grep that `tone="gold"` is never actually invoked anywhere in the app; dead code, not a live bug, left as-is.
- **2 real, previously-unverified failures**: the atoll/destination kicker on the **home hero** (`Hero.tsx`) and the **resort-page hero** (`resorts/[slug]/page.tsx`). Both sit over a photographic hero image + gradient scrim, not a solid color — the earlier assumption that "gold on the dark hero passes, same as the footer" turned out to be wrong: the scrim (`from-aubergine/80 via-aubergine/15 to-transparent`) doesn't reach full darkness where the kicker sits (above the H1, in the lighter part of the gradient), and it varies with the underlying photo. Measured via actual pixel-sampling (a screenshot of the live element, decoded to a canvas, background sampled from the corners) rather than assumed: home hero **3.59:1**, resort hero (Madifushi) **1.86:1** — both real AA failures, confirmed visually with a screenshot crop before touching anything.
  - **Fix**: swapped both to `text-ivory` (matching the already-legible H1 right below them) plus a `[text-shadow:0_1px_3px_rgba(0,0,0,0.45)]` for extra legibility insurance against whatever photo a future resort uses, since this position is inherently more exposed to the underlying image than the H1 lower in the scrim. Re-verified live post-fix: home hero 8.30:1, resort hero 4.99:1 — both now comfortably pass, and a screenshot crop confirms it reads cleanly against the same Madifushi photo that failed before.

This closes the `text-gold`-contrast defect across the entire site — it will not recur from a component this codebase already has; any future gold-on-photographic-hero usage should default to `text-ivory` + the same shadow treatment instead.

## Star / monogram motif work (2026-07-22)

Implemented the spec §4.3 ask to make the four-point star (sparkle) and WP monogram "more prominent and repeated across sections" — this had been outstanding since the brand-rename round, not blocked on the still-pending logo SVG (that only blocks the header *wordmark* itself).

- **`src/components/Sparkle.tsx`**: new reusable inline SVG matching the sparkle glyph baked into `public/images/logos/wp-monogram-*.png` (a generic four-point "sparkle" shape — colored via `currentColor`, so it takes on whatever `text-*` class wraps it).
- **`Kicker.tsx`** (the shared component used by 17 files across every page): the plain leading rule now reads rule → sparkle → label. This alone put the motif on nearly every section header sitewide for one change. `Footer.tsx`'s four column labels were converted from raw `<p className="kicker">` markup to the shared `<Kicker>` component specifically to pick this up too (a net simplification, not just a motif add).
- **Scroll cue**: added to the bottom of the home hero (`Hero.tsx`) — a small bouncing sparkle + line, `motion-safe:animate-bounce` so it's inert under `prefers-reduced-motion` with no extra JS needed.
- **Watermark behind quotes**: a large, very faint (`/10` on light ivory, `/[0.04]` on dark aubergine — light-on-dark reads more visible than dark-on-light at the same opacity, tuned by eye) sparkle sits behind the home `ConceptSection` quote and the About page's benefits quote. `pointer-events-none`, `aria-hidden` via the Sparkle component itself.
- **List bullets**: About page's benefits-list leading rule (`h-px bg-gold`) replaced with a small gold sparkle.
- **Hero kickers**: both the home hero and resort-page hero kickers (raw markup, not the shared `Kicker` component, since they need the photo-legibility text-shadow treatment from the gold-sweep fix above) got the same rule+sparkle treatment by hand, matching the shared component's pattern.
- **Form success states**: all four forms (Register/Partner/Contact/Inquiry) now show a small gold sparkle above the "thank you" title.
- **Favicon**: `src/app/icon.svg` (Next.js app-router convention, auto-wires the `<link rel="icon">` tags, no code changes needed elsewhere) — the sparkle in aubergine, replacing the default Next.js placeholder `favicon.ico` (kept as a legacy fallback, both now serve correctly).

Deliberately *not* touched: the resort/partners-page atoll labels and the tours-page date pills — these are dense multi-card grids (4 resort cards, 5 tour cards) where multiplying the motif per-card risked tipping into clutter rather than reinforcement, per the design skill's own restraint guidance. If the client wants it there too, it's a quick follow-up, not a redesign.

**Found while verifying (pre-existing, unrelated to this change — confirmed by testing against the unmodified code via `git stash`), fixed same day**: at short mobile viewport heights (≤667px, e.g. iPhone SE and similar older/smaller devices), the home hero's kicker and H1 overlapped by a few pixels — the hero's stacked content (kicker, heading, lead, buttons, next-tour pill) exceeded the `h-screen min-h-[720px]` section height at these viewports, and since the section is bottom-anchored (`items-end`), the overflow pushed the top content (kicker) up past the viewport edge into the heading.
- **Fix**: `h-screen min-h-[720px]` → `min-h-screen` on the hero `<section>`. `h-screen` forces a *fixed* 100vh height regardless of content, which is what caused the clipping/overlap; `min-h-screen` only sets a *floor*, so on short viewports where the stacked content needs more room than 100vh, the section now grows to fit it (a few px of extra scroll below the fold) instead of clipping. Desktop and normal-height mobile are visually unaffected — verified section height stays exactly 100vh there since content already fits within it, and the fix was tested across 5 viewport sizes (iPhone SE, an even smaller 320×568, iPhone 12, desktop, and a short 1440×700 window) with the kicker/H1 gap holding at a consistent 24px clearance everywhere.

## Hero photo, destination-neutral headline, wordmark font (2026-07-22)

Three more client-feedback items, done without waiting on pending assets:

- **Home hero photo replaced**: the client flagged the old hero (`hero-so-aerial.jpg`, 1987×2200 — low-res *and* portrait-oriented, an awkward fit for a full-bleed landscape hero) as low quality but hadn't sent a replacement. Rather than wait, picked the best already-available high-res aerial from the raw resort libraries: `Meyyafushi Images and Videos/New photos/0. Aerials/Aerial 1.jpg` (11144×7429 source — professional drone photography, golden-hour light matching the brand's warm palette, landscape framing with open sky at the top for the header/kicker to sit over). Resized via `sips -Z 3200` to `public/images/home/hero-meyyafushi-aerial.jpg`; `Hero.tsx` updated. The old file is left in place unused rather than deleted, in case it's wanted back.
- **Hero headline made destination-neutral**: both EN ("A front-row seat to the Maldives' finest resorts" → "...the world's finest resorts") and RU (parallel fix, "курортов Мальдив" → "курортов мира") — the client felt the specific Maldives naming undercut the site's own new multi-destination framing (Cinnamon, Oman, Kenya). Left the hero's "next tour" pill as-is (`Ближайшая программа · ... · Мальдивы`) since that's a factual statement about the actual next confirmed edition, not a brand-positioning claim.
- **Header wordmark font — interim fix, not the final one**: "WE PRESENT" was set in `font-display` (Playfair Display, serif), which visually clashes with the monogram mark right next to it (a bold geometric sans, per the actual `wp-monogram-*.png` art) — this is the client's "logo font doesn't match" complaint. Switched to `font-sans` (Manrope, semibold) as a same-family interim improvement while the real logo SVG is still pending; swapping to the exact vector wordmark when it arrives is a trivial follow-up, not a redesign.

All verified live post-change: no horizontal overflow, headline text confirmed via DOM query in both locales, wordmark computed `font-family` confirmed as Manrope, and the short-viewport hero fix (previous section) re-verified as still holding with the new, longer EN headline.

## Home intro copy (exact invitation wording) + site-wide em dash removal (2026-07-22)

- **Home intro replaced with the client's exact requested wording** (`home.intro.body` in both dictionaries) — not a paraphrase, copied verbatim from the client's message for EN; RU re-translated to match the new EN structure precisely (added "representatives"/"digital platform innovators"/"key industry professionals"/"cutting-edge"/"new partnerships"/"guarantee measurable results" — all present in the new RU too). Verified via a live screenshot against the requested text word-for-word.
- **Every em dash (`—`) removed from user-facing content, site-wide**: grepped `—` across all of `src` (was 80 hits across 10 files), fixed all instances in the two i18n dictionaries, `lib/resorts.ts`, `lib/tours.ts`, and the four API routes' generated email subjects + the mailer's "From" display name. Replaced with whatever reads most natural per instance — a comma, colon, semicolon, parentheses, or (for short "label — value" pairs like resort travel times) the site's existing `·` separator convention, matching how the hero kicker already used it. **Did not touch en dashes** (`–`, a different character, used correctly for date ranges like "17–23 August 2026") since the ask was specifically about em dashes.
- **4 remaining instances are deliberately untouched**: two JSDoc-style code comments (`Sparkle.tsx`, `resorts.ts`) and two lines in `mailer.ts` (an internal comment and a `console.log` debug string, gated behind missing env vars, never sent anywhere) — none of these render on any page or reach an end user or the client's inbox, so they're outside the scope of "on the website."
- **Verified exhaustively, not spot-checked**: a Playwright script visited all 15 distinct routes in both locales (30 page loads total — home, about, tours list, both tour details, partners, register, become-a-partner, contact, how-it-was, legal, and all 4 resort pages) and searched each page's full rendered text for the `—` character. Zero hits.

## Full feedback re-audit — 3 stale Maldives-only/out-of-date spots fixed (2026-07-22)

Went back through every client feedback source (the original PDF, all three chat rounds, and the client's own replies) and checked each point against the live dictionary/data files directly rather than trusting prior session summaries — found 3 real gaps the destination-neutral and Cinnamon/Kenya work hadn't reached yet, all fixed (both locales):

- **`meta.description`** (the SEO/social-share tag) still said "...experience the Maldives' finest resorts..." — never updated when the hero headline went destination-neutral. Now "...the world's finest resorts...".
- **About page banner H1** ("An invitation to the partners who sell the Maldives every day") — the one page meant to explain the whole multi-destination concept opened with Maldives-only framing. Now "...who sell the world's finest resorts every day", echoing the home hero's own "world's finest resorts" phrase for consistency across pages.
- **About page's concept narrative paragraph was stale**: described the season as "two Maldives tours... followed by an Oman tour in December, with further destinations planned for 2027" — didn't mention Cinnamon Resorts (Oct 2026) or Kenya at all, and wrongly implied everything past Oman was 2027-only. Rewrote to name both and drop the inaccurate "planned for 2027" framing.

Deliberately left alone: the home hero's own quote ("...the partners who sell the Maldives every day...") — that's Daria's client-approved copy in quotation marks, a different thing from unquoted page copy, not unilaterally rewritten.

## RU-rendering confirmation sweep — 1 real bug found + fixed (2026-07-22)

The client's original "Russian version doesn't display correctly" complaint had only ever had one confirmed-and-fixed instance (the header byline wrap) plus a lot of live spot-checking without finding more — asked to actually confirm it page by page rather than assume. Did it properly this time:

- **Automated pass**: a Playwright script hit all 15 routes × 4 viewports (375×667, 390×844, 768×1024, 1440×900) = 60 page loads in RU, checking for horizontal overflow, any text element resolving to a non-Manrope/Playfair font (system-font fallback), heading/paragraph bounding-box overlaps, and console errors.
- **Found one real bug**: the Legal page's H1 ("Политика конфиденциальности и уведомление о cookie") overflowed its container by ~9px at mobile widths (confirmed RU-specific — the EN equivalent's `scrollWidth` matched the viewport exactly, RU's didn't). Root cause: "конфиденциальности" (19 characters, no natural break point) doesn't fit the banner's `max-w-3xl` column at mobile widths, and without `overflow-wrap`, a single unbreakable word overflows its container instead of wrapping — the classic "Russian words run longer than English" failure mode the client described.
- **Fixed systemically, not just the one instance**: this exact banner-H1 className (`font-display mt-5 text-4xl text-ivory sm:text-5xl`) is shared verbatim by 9 pages (Tours, Tour detail, Become a Partner, Contact, Register, How It Was, About, Partners, Legal). Added `break-words` to all 9 rather than patching only the one currently-overflowing page — the other 8 weren't triggering it with their *current* RU copy, but the same failure mode was one long word away from happening on any of them.
- **Re-ran the full 60-page-load automated sweep after the fix**: zero findings. Followed up with full-page screenshots of the highest content-density pages (home, tours calendar, a tour detail page with the itinerary timeline, a resort page, both form pages) at mobile and desktop — all confirmed clean by eye, no overlap, no clipped text, no misalignment.

This is now a genuinely confirmed, not assumed, fix — both the automated structural check and a visual pass agree.

## Content portal (Decap CMS), 2026-07-22

Built the Git-based content portal from spec Section 9.2 (Decap CMS, no SaaS fee, versioned in Git) — the client's own stated top priority. Editors can now change tour dates/itineraries, resort key facts/descriptions/photos, and the home-page intro without a code deploy.

**Data model migration**: `src/lib/tours.ts` and `src/lib/resorts.ts` went from hardcoded TS arrays to server-side loaders (`fs.readdirSync`/`readFileSync`, safe since these are Server Components only) reading `content/tours/*.json` and `content/resorts/*.json` — one file per entity, each with a new `order` field controlling display order (previously implicit in array order). Every other export (`getTour`, `getResort`, `t`, `tl`, all types) is unchanged, so no consuming component needed edits. Same pattern for the home intro block, extracted from the dictionaries into `content/settings/home.json` via a new `src/lib/settings.ts` (`getHomeSettings()`), since it's now portal-editable rather than static UI copy — `en.ts`/`ru.ts` no longer have a `home.intro` key. `content/how-it-was/` exists as an empty (`.gitkeep`) collection, ready for when real trip reports/photos exist post-launch (see the existing "How It Was page intentionally has no images" note above).

**Decap setup**: `public/admin/index.html` + `public/admin/config.yml` (schema for all four collections: tours, resorts, settings/home, how_it_was — every field has an editor-facing `hint` explaining constraints like "must match a resort's Slug field"). `local_backend: true` lets it run fully offline against real repo files via `npx decap-server` (run from the repo root, alongside `npm run dev`) — no GitHub OAuth needed for local editing/testing. Three infrastructure bugs found and fixed getting `/admin` to load at all:
- `src/proxy.ts`'s locale-redirect matcher didn't exclude `/admin`, so it 308'd to `/ru/admin` — added `admin` to the negative-lookahead regex, same treatment as `api`.
- Next.js's `public/` static serving only matches exact filenames (no directory-index auto-resolution like a conventional static host) — bare `/admin` 404'd even with the matcher fixed. Added a `rewrites()` rule in `next.config.ts` mapping `/admin` → `/admin/index.html`.
- Decap's default relative `config.yml` lookup resolves against the current URL; since the page loads at bare `/admin` (no trailing slash), the browser resolved it against `/` instead of `/admin/`, and the CDN-loaded CMS failed silently with a console 404. Fixed with an explicit `<link href="/admin/config.yml" rel="cms-config-url">` in `admin/index.html`.
- One Decap-specific config gap: folder collections default to expecting Markdown+frontmatter, so `tours`/`resorts`/`how_it_was` all showed "No Entries" until `extension: "json"` + `format: "json"` were added to each collection definition.

**Verified end-to-end via Playwright** (not just code review): read path confirmed first — every field of every tour and resort renders correctly in the CMS UI against the real JSON. Write path confirmed second — filled the SO/ Maldives tagline field with a marker string, clicked through Decap's Publish dropdown (`Publish` → `Publish now`, a `<span>`-based custom dropdown, not a native `<button>` — matters if scripting against it again), and confirmed the edit landed in `content/resorts/so-maldives.json` on disk via `decap-server`'s local filesystem proxy, then reverted the test edit. Decap's writes reformat/reorder JSON keys (harmless — JSON key order isn't meaningful, and `JSON.parse` doesn't care), so don't be alarmed by an unrelated-looking diff shape after a real editor save.

**Not yet done — needed before this ships to real, non-technical editors**: a production OAuth backend (GitHub OAuth app + a small serverless token-exchange proxy, replacing `local_backend`) once the client's hosting is finalized — `local_backend: true` only works when someone is running `decap-server` on the same machine as the browser, i.e. local dev/testing only, not a real remote editor workflow. `backend.repo` in `config.yml` is currently set to `shhafuuu/we-present-website`; confirm this is the actual final repo before wiring up OAuth.

**Media upload path verified (2026-07-22)**: uploaded a test image through the CMS's media-library widget (`Choose different image` → `Upload`, which is a hidden `<input type=file>` in the DOM, not a native OS file-picker dialog — use `page.setInputFiles`, not `waitForEvent('filechooser')`, if scripting this again) on a resort's card image field, published, and confirmed the file landed correctly at `public/images/uploads/<filename>` and the JSON's `cardImage` field updated to `/images/uploads/<filename>`. Test edit reverted, uploads folder removed after.

**Important, non-obvious behavior found during that test — content changes don't appear live until the next process restart/rebuild**: `tours.ts`/`resorts.ts` read their JSON files via a plain `fs.readFileSync` at module scope, which is invisible to Turbopack's file-watch graph (it only tracks static `import`s, not runtime `fs` calls) — so a change published through the CMS does *not* show up on a page that's already being served by a running process. Confirmed directly: the uploaded image was missing from the homepage resorts grid immediately after publishing, and only appeared after restarting `next dev`. This is not a bug to fix so much as a fact to design the editorial workflow around:
- **In local dev**, if you or an editor test a CMS edit and don't see it, restart `next dev` before assuming the write failed — check the actual JSON file under `content/` first, which is the reliable source of truth for "did the save work."
- **In production**, since resort/tour pages are pre-rendered at build time (`generateStaticParams`, SSG — not `output: "export"`, but still baked in per-build, not read live per-request), a CMS publish will only reach the live site after the next `next build` + redeploy. This is standard for a Git-CMS + SSG pairing (Decap is normally used with a CI webhook that rebuilds on every push to `main`) — but there's no such rebuild trigger wired up yet, since it depends on whatever hosting platform the client lands on. Most static hosts (Vercel, Netlify, AWS Amplify) do this automatically the moment a repo is connected; a custom AWS/EC2 setup would need an explicit webhook. Flag this to the client alongside the OAuth-backend item once hosting is decided — "editors publish, but the site doesn't update until X" needs to be an explicit, understood part of their workflow, not a surprise.

## Client feedback round 2 (v1.9 spec) — Meyyafushi fix, casing sweep, hero photo, 2026-07-22

First slice of the v1.9 spec's unblocked items (Destinations restructure, RU/EN form split, Security hardening, and About rebuild are separate, larger rounds — see below and "Current scope").

- **Meyyafushi atoll corrected**: "Raa Atoll"/"Атолл Раа" → "Lhaviyani Atoll"/"Атолл Лавияни" — same defect class as the earlier Madifushi/Meemu fix, same fix pattern (atoll field, key-facts location, and the story paragraph that also named the atoll by hand, EN+RU, all in `content/resorts/meyyafushi.json`). Grepped for any other stray "Raa"/"Раа" references sitewide after — none.
- **Casing sweep**: RU's hero lead and concept-quote strings had "Coati Global Sales Agency" (mixed case) instead of the correct all-caps "COATI Global Sales Agency" — EN was already correct everywhere (verified by grep before touching anything). Fixed both RU strings in `ru.ts`; the small-UI "от Coati" kicker string is intentionally lowercase per the brand rules and was left alone.
- **Home hero photo replaced again**: swapped to the client's actual delivered destination-neutral image (an open-ocean aerial, no visible land/resort — `public/images/home/hero-open-ocean.jpg`, converted from the source PNG via `sips`). The previous Meyyafushi-aerial interim photo is left in place unused, same "don't delete, might be wanted back" pattern as before.
- **Found and fixed a real contrast regression from the photo swap, verified live, not assumed**: pixel-sampled the hero kicker's actual on-screen background (screenshot → canvas → average RGB → WCAG contrast formula, same method as the earlier gold-sweep round) and got **2.61:1** against the ivory kicker text — a real AA failure. Root cause: the new photo is much brighter/whiter (sea foam) in the top-left region than the previous golden-hour aerial that the existing scrim opacities were tuned against, so a scrim strong enough for one photo isn't automatically strong enough for another — **this class of check has to be redone on every hero photo swap, it doesn't carry over.** Fixed by strengthening the top-of-hero scrim gradient in `Hero.tsx` (`bg-gradient-to-b from-aubergine/30 via-transparent` → `from-aubergine/95 via-aubergine/50`), iterating against live pixel-sampled measurements until both the kicker (5.18:1) and H1 (4.95:1) comfortably passed, then confirming by eye by iterating until the effect still reads as a light photo wash rather than a muddy overlay — verified via full-page screenshots at desktop and mobile in both locales.

## Security hardening (v1.9 spec §12), 2026-07-23

Second slice of the v1.9 spec work. Covers everything in §12.2 that doesn't depend on a hosting decision (SES migration, CloudWatch abuse monitoring, and the production CMS OAuth/2FA process items all stay blocked on that, per "Current scope" below).

- **Security headers + CSP** (`next.config.ts`): added a `headers()` block sending CSP, HSTS, `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, and a restrictive `Permissions-Policy` on every route. **Deliberately not using a nonce-based CSP** — Next's own docs (`node_modules/next/dist/docs/01-app/02-guides/content-security-policy.md`) are explicit that nonces require every page to be dynamically rendered, which would force this entire SSG site (every route is prebuilt via `generateStaticParams`) into per-request server rendering for a security property it doesn't need: no user-submitted content is ever rendered back into a page anywhere on the site (forms only ever go to email), so there's no stored/reflected-XSS surface for `'unsafe-inline'` script execution to actually exploit. Went with Next's own documented "Without Nonces" pattern instead. `script-src` allows `https://unpkg.com` for the Decap CMS bundle; `connect-src` allows `http://localhost:8081` (the local `decap-server` proxy) only when `NODE_ENV !== "production"`, verified via a real `next start` production-mode server showing that origin correctly absent from the header. **Found and fixed a real breakage while testing, not assumed away**: an initial strict `script-src 'self' https://unpkg.com` (no `unsafe-inline`) broke hydration completely in dev (`Invariant: Expected a request ID to be defined ... self.__next_r`) and threw dozens of CSP-violation console errors in prod-mode too — Next/React inject their own inline bootstrap scripts that a strict CSP blocks unless you either add a nonce (ruled out above) or `'unsafe-inline'` (what Next's own non-nonce guide recommends). Fixed by adding `'unsafe-inline'` to `script-src` (and `'unsafe-eval'`, dev-only, per the same Next doc's note that React's dev-mode stack-trace reconstruction needs it). Verified with zero console/page errors across 7+ pages including the Gallery lightbox, in both `next dev` and a real `next start` production server, and re-confirmed the CMS admin flow (login → browse → open an image field) still works cleanly under the new headers.
- **Upload hardening** (`src/lib/uploads.ts`): added real magic-byte (file signature) checks on top of the existing extension + size checks — `validateUpload` is now async, reads the first 8 bytes of each upload, and rejects anything whose actual content doesn't match its claimed extension (e.g. a `.txt` renamed to `.pdf`), regardless of the client-supplied `file.type`, which is never trusted for validation. `.doc`/`.xls` share one legacy OLE2 signature and `.docx`/`.xlsx` share one OOXML/zip signature — checking proves "this is a genuine file of that general family," not the exact sub-extension, which is the standard/practical depth for this kind of check. Also added `validateUploadBatch()` — caps at 5 files and 40MB combined per submission (`register`'s two fixed fields will never hit this, `partner`'s variable-length file list could have without a cap). Verified live: a plain-text file renamed to `.pdf` was correctly rejected ("doesn't look like a valid PDF file"), a genuine image passed and was logged, and 6 files against the partner route's 5-file cap was correctly rejected.
- **Submission time-trap** (`src/lib/antiSpam.ts`, spec's explicit ask): each of the four forms now records its own mount time (`useState(() => Date.now())`) and round-trips it back as `formLoadedAt` — a hidden input for the two `FormData` forms (Register, Partner), a JSON field for the two JSON forms (Contact, Inquiry). Anything submitted less than 2 seconds after the form loaded gets the same silent-success treatment as the honeypot (`{ok: true, delivered: false}`, nothing logged or emailed) — real users can't type a form that fast, but a bot filling every field programmatically and submitting instantly can. A missing/malformed timestamp does *not* trigger the trap (avoids false-blocking a real visitor over a client-side quirk; the honeypot already covers bots that skip fields outright). Verified live: an instant submission was silently dropped, an identical one with a 5-second-old timestamp was logged normally, and a full real-browser Playwright run through the Register form's actual UI (fill → realistic pauses → submit) still reached the thank-you state with zero console errors.
- **Deeper input validation** (`src/lib/validate.ts`): `isValidEmail()` (format + 254-char RFC cap) now gates every route's email field before it's used as the `replyTo` header passed to Nodemailer — this is the spec's "validate the reply-to address before using it as a header" ask; Nodemailer builds MIME structurally rather than via raw string concatenation so header-injection isn't a live risk here regardless, but rejecting malformed input server-side is the defense-in-depth the spec asked for anyway. `isWithinLength()` caps every text field per route (names/hotel/agency 200 chars, phone 50, message/comments 5000, website 500) — chosen to be generous for real use, tight enough to block pathological payloads. Verified live: a malformed email and a 300-character name were both correctly rejected with clear error messages, and the existing required-field checks and error message wording were left untouched (only added checks, didn't rewrite what already worked).
- **Dependency scanning**: added `.github/dependabot.yml` (weekly npm scan of `web/`, dev-dependencies grouped into one PR so routine devDependency bumps don't spam individual PRs). `npm audit` still reports the same 3 pre-existing transitive vulnerabilities inside Next's own bundled `postcss`/`sharp` as before (see the CMS section above) — the only fix `npm audit` offers is still a destructive downgrade to `next@9.3.3`, so left as-is; Dependabot will now surface it automatically once Next itself ships a real fix. CI-integrated `npm audit` (spec's other ask under this line item) isn't set up since there's no CI pipeline yet — nothing to hook it into until one exists.

All verified live end-to-end (Playwright against a running dev server for the CSP/form checks, a real `next start` production server for the header/CSP prod-mode check, and `curl` for the fast, precise-timing security-control tests), not just code review — full `tsc`/lint/build pass throughout.

## Resorts → Destinations restructure + RU/EN form split (v1.9 §6.4a, §8.6), 2026-07-23

Third slice of the v1.9 spec work. **Oman and Kenya are included as visible "coming soon" cards** on the Destinations landing page, not omitted — they just don't have their own page yet since neither has a confirmed resort or partner roster.

- **New content model**: `content/destinations/*.json` (new `src/lib/destinations.ts` loader, same `fs.readdirSync`/`readFileSync` pattern as `tours.ts`/`resorts.ts`). A `Destination` has `status: "active" | "coming-soon"` — only `active` gets `generateStaticParams`'d into a real page; `coming-soon` ones (`oman.json`, `kenya.json`) are minimal (slug/order/status/name only, no hero/intro) and only ever render as a non-clickable card. `maldives.json` is `active` with a real hero image (reused `hero-meyyafushi-aerial.jpg`, previously unused since the home hero moved to the new ocean photo) and drafted intro copy (flagged below — unapproved, same as other interim copy this project). Every resort gained a `destinationSlug: "maldives"` field (`Resort` type in `resorts.ts` updated accordingly); `getResortsForDestination()`/`getToursForDestination()` in the new lib do the filtering. **Tours don't get their own `destinationSlug`** — they already have a free-text `destination: {en, ru}` display field used for calendar grouping, so a programme is considered to "visit" a destination when that field's English value matches the destination's English name. Lightweight string coupling, deliberately not over-engineered into a second slug field; noted in a code comment as the thing to revisit if it ever causes a real mismatch.
- **New routes**: `/[locale]/destinations` (landing — grid of destination cards, Maldives real+clickable, Oman/Kenya dashed-border "coming soon" cards matching the existing 2027-tours-card pattern) and `/[locale]/destinations/[slug]` (detail — hero, partner resorts grid, and a programmes list filtered to that destination; `generateStaticParams` only builds `maldives`, confirmed via the build output showing exactly one destination detail page per locale). Both fully bilingual, new `destinationsPage` dictionary block in both `en.ts`/`ru.ts`.
- **Nav rename**: `nav.resorts` → `nav.destinations` (key renamed, not just the label, since its meaning changed) in both dictionaries; `Header.tsx`/`Footer.tsx` now link to `/destinations` instead of the old `/#resorts` home-page anchor. The resort detail page's "All Resorts" back-link and the home page's `ToursTimeline` "See the Resorts on Tour" button were repointed the same way — grepped for any remaining `#resorts` references afterward, none left. The home page's `ResortsGrid` section itself is deliberately left showing the same 4 resort cards as before (visually and functionally identical today, since Maldives is still the only populated destination) — added a "View All Destinations" button below it rather than restructuring the section, to avoid unnecessary risk to an already fully-audited component for a change that wouldn't currently look any different anyway.
- **CMS**: new `destinations` collection in `public/admin/config.yml` (active/coming-soon select, hero image + intro marked required-before-going-active via a field hint, since Decap has no cross-field conditional validation) and a `destinationSlug` select field added to the `resorts` collection. Verified live in the CMS: all 3 destinations list correctly with accurate status summaries, and the Maldives entry's full field set (including the hero image) reads back correctly.
- **RU/EN form split** (spec §8.6): `/register` now branches by locale — RU still shows the full `RegisterForm` (unchanged); EN shows a new `RegisterEnContactBlock` (a simple card with drafted copy + a `mailto:` button to the placeholder contact email) instead, with its own EN-specific banner copy above it, since the RU-registration-focused banner text ("Register your interest for the next edition...") wouldn't make sense leading into a contact block. `/become-a-partner` (hotel partnership requests) is untouched — the spec's ask was specifically about the agent-registration form, not the separate partner-inquiry one. This also incidentally satisfies the spec's related security note ("keep the upload endpoint unreferenced for the EN flow") — the EN page no longer renders any file-upload UI, though `/api/register` itself isn't locale-gated (rate-limiting/honeypot/time-trap protect it regardless of how it's reached).
- **Found and fixed one real content bug while verifying, not left in**: the destination detail page's "Partner Resorts" section initially had its H2 repeat the destination name ("Maldives") — identical to the H1 right above it, which slipped through since it read fine in isolation but was redundant in context. Caught during a routine heading-outline check (`document.querySelectorAll("h1,h2,h3")`), same verification method used throughout this project. Replaced with distinct copy ("Four islands, one itinerary" / "Четыре острова, один маршрут").
- **A "blank section" screenshot during verification was a false positive, not a bug**: an initial `fullPage` screenshot of the Maldives destination page showed an empty lavender band where the Programmes section should be. Per this file's own "Visual verification" guidance, re-checked with incremental scrolling (`Reveal`'s `whileInView` needs the section actually scrolled into view) — all three Maldives-linked tours (Tour 1, Tour 2, Cinnamon Resorts) rendered correctly, and Oman/Kenya tours were correctly excluded. Worth restating since this exact failure mode keeps recurring across rounds: a single full-page screenshot without incremental scroll is not a reliable way to check this site's animated sections.

**Interim Maldives destination-intro copy — flagged as unapproved, same as other interim copy this project has drafted (hero photo, wordmark font, Legal page draft notice)**: written from known facts already in the data (the 3 atolls the 4 resorts actually sit in, TTM's role) rather than invented marketing language, but hasn't been through the client. Flag before launch, same as the Russian translation quality across the site generally.

All verified live: full `tsc`/lint/build pass, a real production build confirming `generateStaticParams` only built the one active destination, Playwright screenshots of both locales' Destinations landing + detail pages, a heading-outline check on all three new/changed pages, a zero-console-error mobile sweep across 6 page/locale combinations, and a live CMS check of the new collection.

## About page rebuild (v1.9 §6.3, Appendix B.7), 2026-07-23

Fourth and final planned slice of the v1.9 spec work (the remaining items are all blocked on client input — see "Current scope" below).

- **Opening statement rewritten**: the old "concept" section ("A B2B programme, not a marketing event") replaced with the platform-positioning copy from Appendix B.7 ("An integrated platform, not a single event") — adapted from the spec's own source copy for both EN and RU, restructured into two shorter sentences instead of the spec's longer em-dash-heavy original to match this site's existing em-dash-free-copy rule (see the earlier "site-wide em dash removal" round) rather than reproducing the spec doc's punctuation verbatim. Same `AlternatingBlock` component and image as before, kicker relabeled "The Platform".
- **New Value Grid section**: the six value points from spec §1 (destination/brand awareness, industry connections, media/digital exposure, sales growth, long-term partnerships, measurable results) as a 6-card grid, each with a short original label + one-line body (not copied from the spec, which only lists the six themes as sentence fragments — written to actually read as content). Sits directly below the opening statement, per the spec's recommended page flow.
- **New Project Cases section**: since real case content is explicitly listed as "Pending" in Appendix D, built honestly as a coming-soon state (dashed-border card, same visual pattern as the home page's `HowItWas` teaser) rather than inventing placeholder cases — explains what will appear (case types, activities, results) and links to `/how-it-was`, which is where the spec says cases eventually surface. This also covers the spec's separate "review blocks" ask (participant/partner testimonials) — folded into the same coming-soon section rather than a second near-identical empty state, since both are blocked on the same missing content.
- **Section background rhythm re-balanced**: inserting two new sections shifted what would otherwise have been two adjacent same-background sections (a real defect class this project has fixed before — "white and lilac look similar," "elements blend into background"). Re-alternated ivory/lavender-mist through the whole page and swapped the Selection Model cards from `soft-lilac/40` to `ivory` + `shadow-card` once that section's background became lavender-mist, so the panels stay visually distinct from whatever they sit on. Verified by tracing the full section list, not just eyeballing the render.
- **`selectionModel`** ("Merit-based, driven by room-nights", kept from v1.8 per the spec's explicit instruction) and the existing **`benefits`** ("Benefits for Agents") section are both left in place and unchanged in content — the value grid is platform-wide value, `benefits` is agent-specific incentive copy, and they're complementary rather than redundant, so nothing was deleted.

**Interim copy — flagged as unapproved, same pattern as the Destinations intro copy and other interim copy this project has drafted**: the opening statement, all six value-grid items, and the cases coming-soon copy are all original/adapted text that hasn't been through client review. Flag before launch alongside the other drafted copy already noted in this file.

All verified live, not just code review: a full incrementally-scrolled screenshot in both locales (avoiding the known `Reveal` false-blank-section trap), a clean H1→H2→H3 heading outline with no duplicates or skips, zero console errors, zero horizontal overflow on mobile in either locale, and a direct `document.body.innerText` check confirming no em dashes made it into the new copy in either language.

## Current scope / not yet implemented
- **Hosting / OAuth backend**: AWS deployment and Russia-reachability testing from spec Section 10 are unstarted — client is handling domain/hosting setup directly. The CMS's production OAuth backend, SES email migration, CloudWatch abuse monitoring, and CI-integrated `npm audit` (spec §12.2) are all blocked on that same hosting/CI decision.
- **Pending client assets** (spec v1.9 Appendix D): logo SVG (blocks finalizing the header wordmark's exact typeface and full logo-standardization work), hotel website URLs, parent-brand logo/link and footer contact details (phone/office/Instagram), confirmation of the "TTM Tier 1/2" naming + Tour 2 per-resort nights + Madifushi inclusion, real project-cases content (the About page's cases section is built and waiting, just showing a coming-soon state), Oman/Kenya resort and partner rosters (needed before those destinations can go from "coming soon" to a real page), and clean SVG/transparent-PNG exports of the Meyyafushi/Fushifaru (currently `.ai`) and Madifushi (currently `.psd`) logo source files.
- **All four planned v1.9 slices are now complete** (Meyyafushi/casing/hero fixes, security hardening, Destinations restructure + RU/EN form split, About rebuild). Everything remaining in the v1.9 spec is blocked on client-supplied content or the hosting decision — see the two bullets above.

## Visual verification

There's no interactive browser in this environment. Playwright (`devDependencies`) plus its Chromium binary are installed for headless screenshot verification — write a throwaway `.mjs` script under `web/` (so it resolves `node_modules`), launch `chromium`, `goto` the dev server, and `page.screenshot()`. Scroll incrementally (e.g. 200–300px steps with short waits) rather than jumping straight to a scroll position — `Reveal`'s `whileInView` triggers are easy to miss with large scroll jumps, producing false-looking "blank section" screenshots that aren't real bugs.
